import Header from "./header/Header";

const Conversation = () => {
  return (
    <div className="flex-1 bg-(--bg-primary) min-h-screen relative">
      <Header />
    </div>
  );
};

export default Conversation;
