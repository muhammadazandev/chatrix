const Loader = () => {
  return (
    <div className="text-center">
      <div className="inline-block animate-spin rounded-full h-8 w-8 border-b-3 border-r-3 border-[rgb(var(--accent-color-primary))] mb-4" />
    </div>
  );
};

export default Loader;
